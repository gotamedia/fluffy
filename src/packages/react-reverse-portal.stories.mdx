import { Source, Meta } from '@storybook/addon-docs'

import * as ReversePortals from './react-reverse-portal'

<Meta
    title={'Packages/react-reverse-portal'}
    parameters={{
        options: {
            showToolbar: false
        }
    }}
/>

## About
For more info please refer to [react-reverse-portal official DOCs](https://github.com/httptoolkit/react-reverse-portal)

## Usage

<Source
    language={'tsx'}
    code={`
import { ReversePortals } from '@gotamedia/fluffy'
import * as ReversePortals from '@gotamedia/fluffy/packages/react-reverse-portal'
`}
/>

## Content

<Source
    language={'json'}
    code={`${(() => {
        const content = {}
        Object.entries(ReversePortals).forEach((item) => {
            content[item[0]] = typeof item[1]
        })
        return JSON.stringify(content, null, 4)
    })()}`}
/>