import { Source, Meta } from '@storybook/addon-docs'

import * as Popmotion from './popmotion'

<Meta
    title={'Packages/Popmotion'}
    parameters={{
        options: {
            showToolbar: false
        }
    }}
/>

## About
The animator’s JavaScript toolbox.

For more info please refer to [Popmotion official DOCs](https://popmotion.io/)

## Usage

<Source
    language={'tsx'}
    code={`
import { Popmotion } from '@gotamedia/fluffy'
\nPopmotion.animate({
    ...
})
`}
/>

## Content

<Source
    language={'json'}
    code={`${(() => {
        const content = {}
        Object.entries(Popmotion).forEach((item) => {
            content[item[0]] = typeof item[1]
        })
        return JSON.stringify(content, null, 4)
    })()}`}
/>