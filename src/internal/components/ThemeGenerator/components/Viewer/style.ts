import styled from 'styled-components'

const Wrapper = styled.div`
    width: 70%;
    display: flex;
    gap: 10px;
    flex-direction: column;

    * {
        margin: 0 auto;
    }
`

export {
    Wrapper
}