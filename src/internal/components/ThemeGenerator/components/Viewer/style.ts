import styled from 'styled-components'

const Wrapper = styled.div`
    width: 70%;
    display: flex;
    gap: 10px;
    flex-direction: column;
    padding: 25px;

    * {
        margin: 0 auto;
    }
`

export {
    Wrapper
}