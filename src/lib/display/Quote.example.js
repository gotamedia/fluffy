import { Quote, QuoteText, QuoteByline } from './Quote'

const Example = () => (
    <Quote>
        <QuoteText>It's so fluffy!</QuoteText>
        <QuoteByline>- Agnes</QuoteByline>
    </Quote>
)

export default Example