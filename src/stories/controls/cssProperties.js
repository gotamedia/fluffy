const overflow = ['visible', 'hidden', 'clip', 'scroll', 'auto']

const cssProperties = {
    overflow,
    overflowY: overflow,
    overflowX: overflow,
    display: ['inherit', 'none']
}

export default cssProperties