import { css } from 'styled-components'

const tiny = css`
    height: 24px;
    font-size: 12px;
`

const small = css`
    height: 32px;
    font-size: 14px;
`

const normal = css`
    height: 40px;
    font-size: 14px;
`

const big = css`
    height: 48px;
    font-size: 18px;
`

const huge = css`
    height: 50px;
    font-size: 20px;
`

const sizes = {
    tiny,
    small,
    normal,
    big,
    huge
}

export default sizes