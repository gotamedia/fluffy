import styled from 'styled-components'

const Wrapper = styled.div`
    position: relative;
    width: 720px;
    height: 405px;
    display: flex;
    flex-direction: column;
`

export {
    Wrapper
}