import { css } from 'styled-components'

const normal = css`

`

const dim = css`
    background-color: rgba(0, 0, 0, 0.18);
`

const variants = {
    normal: normal,
    dim: dim
}

export default variants