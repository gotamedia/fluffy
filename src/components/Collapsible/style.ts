import styled from "styled-components"

const Container = styled.div``

const Wrapper = styled.div`
	height: 0px;
	overflow: hidden;
`

const ContentWrapper = styled.div``

export { Container, Wrapper, ContentWrapper }
