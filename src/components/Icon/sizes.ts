import { css } from 'styled-components'

const micro = css`
    height: 12px;
    width: auto;
`

const tiny = css`
    height: 16px;
    width: auto;
`

const small = css`
    height: 20px;
    width: auto;
`

const normal = css`
    height: 24px;
    width: auto;
`

const big = css`
    height: 28px;
    width: auto;
`

const huge = css`
    height: 36px;
    width: auto;
`

const sizes = {
    micro,
    tiny,
    small,
    normal,
    big,
    huge
}

export default sizes