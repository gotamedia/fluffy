import { css } from 'styled-components'

const tiny = css`
    height: 16px;
    width: auto;
`

const small = css`
    height: 20px;
    width: auto;
`

const normal = css`
    height: 24px;
    width: auto;
`

const big = css`
    height: 28px;
    width: auto;
`

const huge = css`
    height: 36px;
    width: auto;
`

const sizes = {
    tiny,
    small,
    normal,
    big,
    huge
}

export default sizes