import { css } from 'styled-components'

const tiny = css`
    min-width: 18px;
    height: 18px;
`

const small = css`
    min-width: 20px;
    height: 20px;
`

const normal = css`
    min-width: 24px;
    height: 24px;
`

const big = css`
    min-width: 26px;
    height: 26px;
`

const huge = css`
    min-width: 28px;
    height: 28px;
`

const sizes = {
    tiny,
    small,
    normal,
    big,
    huge
}

export default sizes