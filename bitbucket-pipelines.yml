image: node:10.16.0
pipelines:
  default:
    - step:
        name: Build and Test
        script:
          - yarn
          - yarn build
    - step:
        name: Publish
        deployment: production
        script:
          - yarn global add standard-version
          - yarn release
          - pipe: atlassian/npm-publish:0.2.0
            variables:
              NPM_TOKEN: $NPM_TOKEN
